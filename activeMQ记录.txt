
目前消息中间件的种类很多，如交易管理中间件、面向java应用的Web应用服务器中间件，而消息传输中间件MOM是其中的一种。
它简化了应用之间数据的传输，屏蔽底层异构操作系统和网络平台，提供一致的通讯标准和应用开发，确保分布式计算网络环境下可靠的、跨平台的信息传输和数据交换。
它基于消息队列的存储-转发机制，并提供特有的异步传输机制，能够基于消息传输和异步事务处理实现应用整合与数据交换。


队列：
1.本地队列
	本地队列按照功能可划分为初始化队列、传输队列、目标队列和死信队列。
	初始化队列用作消息触发功能；
	传输队列只暂存待传的消息，条件许可情况下，通过管道将消息传送到其它的队列管理器；
	目标队列是消息的目的地，可以长期存放消息；
	如果消息不能送达目标队列，也不能再路由出去，则被自动放入死信队列；
2.别名队列&远程队列
	只有一个队列定义，用来指定远端队列管理器的队列。使用了远程队列，程序就不需要知道目标队列的位置；
	
	

JMS概述：
	JMS即Java消息服务，是Java EE 的标准/规范之一。这种规范（标准）指出：消息发送应该是异步的、非阻塞的。
也就是说消息的发送者发送完消息后就直接返回了，不需要等待接收者返回后才能返回，发送者和接受者可以说是互不影响。
所以这种规范（标准）能够减轻或消除系统瓶颈，实现系统之间去除耦合，提高系统的整体可伸缩性和灵活性。JMS只是Java EE 中定义的一组标准API，
它自身并不是一个消息服务系统，它是消息传送服务的一个抽象，也就是说它定义了消息传送的接口而没有具体实现。

ActiveMQ概述：
	我们知道JMS只是消息服务的一组规范和接口，并没有具体实现，而ActiveMQ就是JMS规范的具体实现；它是Apache下的一个项目，
采用Java语言开发；是一款非常流行的开源消息服务器；


ActiveMQ与JMS关系:
	我们知道，JMS只是定义了一组有关消息传送的规范和标准，并没有真正实现，也就说JMS只是定义了一组接口而已；
就像JDBC抽象了关系数据库访问、JPA抽象了对象与关系数据库映射、JNDI抽象了命名目录服务访问一样，JMS具体的实现由不同的消息中间件厂商提供，
比如Apache ActiveMQ就是JMS规范的具体实现，Apache ActiveMQ才是一个消息服务系统，而JMS不是。

通道：
	通道是MQ系统中队列管理器之间传递消息的管道，它是建立在物理的网络连接之上的一个逻辑概念，也是MQ产品的精华。
在MQ中，主要有三大类通道类型，即消息通道，MQI通道和Cluster通道。
消息通道是用于在MQ的服务器和服务器之间传输消息的，需要强调指出的是，该通道是单向的，它又有发送(sender), 接收(receive), 请求者(requestor), 
服务者(server)等不同类型，供用户在不同情况下使用。
MQI通道是MQ Client和MQ Server之间通讯和传输消息用的，与消息通道不同，它的传输是双向的。
群集(Cluster)通道是位于同一个MQ 群集内部的队列管理器之间通讯使用的。


JMS常见对象：
	Provider：纯 Java 语言编写的 JMS 接口实现（比如 ActiveMQ 就是）
	Domains：消息传递方式，包括点对点（P2P）、发布/订阅（Pub/Sub）两种
	Connection factory：客户端使用连接工厂来创建与 JMS provider 的连接
	Destination：消息被寻址、发送以及接收的对象
	
JMS通用规范 使用和创建应用JMS的通用步骤如下:
	1.获取连接工厂
	2.使用连接工厂创建连接
	3.启动连接
	4.从连接创建会话
	5.获取 Destination
	6.创建 Producer，或
	7.创建 Producer
	8.创建 message
	9.创建 Consumer，或发送或接收message发送或接收 message
	10.创建 Consumer
	11.注册消息监听器（可选）
	12.发送或接收 message
	13.关闭资源（connection, session, producer, consumer 等)

ActiveMQ的使用

	2.1 ActiveMQ环境搭建:

		1.ActiveMQ运行需要Java的支持，首先需要配置Java环境变量；
		2.切换到解压后的activemq的bin目录下 cd / usr / local / apache - activemq - 5.15.2 去启动
		3.切换到bin目录下，启动：./activemq start ;关闭： . / activemq stop
		4.启动后有两个端口号，一个是web控制台:8161，一个是消息服务broker连接端口：61616
		5.web管理控制台admin URL地址：http : // localhost : 8161 默认登录账号 admin 密码 admin，注意：Linux防火前要关闭 ;
		通过这个地址可以即时访问交互信息.  消息服务broker URL地址 : tcp : // localhost : 61616


		
JMS定义了五种不同的消息正文格式
· StreamMessage -- Java原始值的数据流
· MapMessage--一套名称-值对
· TextMessage--一个字符串对象
· ObjectMessage--一个序列化的 Java对象
· BytesMessage--一个未解释字节的数据流
 